"""
IP geolocation and reputation utilities.
Uses ipinfo.io API — token loaded from environment.
Build target: Month 1 (basic) → Month 2 (integrated with DB caching).
"""
# TODO Month 1: implement get_ip_info(), is_known_tor_exit(), is_private_ip()
